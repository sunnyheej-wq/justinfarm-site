# 관리자 글 편집기 업데이트 가이드

## 📋 변경 사항 요약

관리자 글 편집기가 완전히 개선되어 이제 **일반 블로그 편집기처럼** 작동합니다.

### ✅ 완료된 개선사항

1. **통합 편집 인터페이스**
   - 제목, 본문, 이미지를 하나의 편집기 안에서 모두 편집
   - "수정" 버튼 클릭 시 모든 요소가 한 페이지에 표시됨

2. **WYSIWYG 편집기**
   - `contenteditable` 기반의 실시간 렌더링
   - 기존 본문 HTML이 그대로 표시됨
   - 마크다운 변환 제거 - 순수 HTML로 저장

3. **이미지 삽입 방식 (3가지)**
   - ✨ **복사-붙여넣기**: Ctrl+V로 이미지 직접 붙여넣기
   - 📁 **파일 선택**: "🖼️ 이미지 삽입" 버튼으로 PC에서 파일 선택
   - 미리보기: 실시간으로 본문 위치에 이미지 표시

4. **이미지 관리**
   - 이미지 클릭 시 선택 표시 (호버 상태에서 "삭제" 버튼 표시)
   - "삭제" 버튼으로 불필요한 이미지 제거
   - 모든 이미지는 **Base64 data URL**로 본문 HTML에 포함

5. **HTML 저장 및 렌더링**
   - 본문이 순수 HTML로 저장됨
   - 블로그 페이지에서 이미지와 함께 정상적으로 렌더링됨
   - `postToHtml()` 함수가 HTML을 escape하지 않음 (실제 렌더링)

6. **기존 기능 유지**
   - 관리자 로그인 ✓
   - 발행/예약/임시저장 ✓
   - 미리보기 ✓
   - HTML 다운로드 ✓
   - 이미지 관리 탭 (홈페이지 상품 사진 URL 관리) ✓

## 📝 사용 방법

### 1️⃣ 새 글 작성하기

```
관리자 페이지 → "새 글 작성" 버튼 클릭
```

작성 화면에서:
- **제목**: 글 제목 입력
- **슬러그**: URL 경로 (자동 생성 가능)
- **카테고리**: 드롭다운에서 선택
- **상태**: 발행됨/예약됨/임시저장 선택
- **유형**: 가이드/비교/최신/페이지 선택
- **본문**: 아래 편집기에서 작성

### 2️⃣ 이미지 삽입하기

편집기 위의 "🖼️ 이미지 삽입" 영역을 참고하세요:

#### 방법 1: 복사-붙여넣기 (가장 빠름)
```
1. PC에서 이미지 파일 복사 (Ctrl+C)
2. 본문 편집기에서 원하는 위치 클릭
3. Ctrl+V 붙여넣기
4. 이미지가 자동으로 본문에 삽입됨
```

#### 방법 2: 파일 선택 버튼
```
1. 본문 위의 "🖼️ 이미지 삽입" 버튼 클릭
2. PC의 이미지 파일 선택
3. 현재 커서 위치에 자동 삽입
```

#### 방법 3: 드래그 앤 드롭 (선택)
```
1. 파일 탐색기에서 이미지 파일을 본문 편집기로 드래그
2. 자동으로 본문에 삽입됨
```

### 3️⃣ 이미지 편집 및 삭제

#### 이미지 보기/삭제
```
1. 본문의 이미지에 마우스 오버
2. 이미지 우측 상단에 "삭제" 버튼 표시
3. "삭제" 버튼 클릭으로 이미지 제거
```

#### 이미지 순서 변경
```
1. 현재 UI에서 직접 드래그는 미지원
2. 대안: 이미지 삭제 후 다시 삽입
```

### 4️⃣ 기존 글 수정하기

```
관리자 페이지 → 글 목록 → "수정" 버튼 클릭
↓
기존 제목, 본문, 이미지가 모두 편집기에 표시됨
↓
필요한 수정 후 "저장" 또는 "저장 후 발행"
```

**주의**: 기존 발행된 글의 본문 HTML을 읽어올 때:
- 이전 마크다운 형식은 더 이상 자동 변환되지 않음
- 기존 HTML 이미지는 그대로 렌더링됨
- 새 이미지는 Base64 data URL로 저장됨

### 5️⃣ 글 저장하기

편집 완료 후 하단의 버튼 선택:

| 버튼 | 기능 |
|------|------|
| **저장** | 현재 상태로 저장 (발행됨/예약됨/임시저장) |
| **저장 후 발행** | 저장 후 즉시 발행됨 상태로 변경 |
| **미리보기** | 새 탭에서 렌더링 결과 확인 |
| **HTML 다운로드** | 완성된 HTML 파일 다운로드 |

## 🔧 기술 세부사항

### 저장되는 데이터 구조

```javascript
{
  id: "string",                    // 글 ID
  title: "string",                 // 글 제목
  slug: "string",                  // URL 슬러그
  category: "string",              // 카테고리
  type: "guide|comparison|latest|page", // 글 유형
  status: "published|draft|scheduled",  // 발행 상태
  body: "<html>...</html>",        // HTML 본문 (이미지 포함)
  image: "data:image/...",         // Base64 또는 URL
  description: "string",           // 메타 설명
  summary: "string",               // 요약 (결론)
  faqs: [["질문", "답변"]],        // FAQ 배열
  updatedAt: "YYYY-MM-DD",         // 수정 날짜
  scheduledAt: "YYYY-MM-DDTHH:MM"  // 예약 발행 시간
}
```

### 이미지 저장 방식

- **Base64 Data URL**: `data:image/jpeg;base64,...`
- 크기 제한: 브라우저 localStorage 한계 (~5-10MB)
- 이미지가 매우 클 경우 브라우저 성능 저하 가능
- 현재는 클라이언트 저장만 지원 (서버 업로드 미지원)

### HTML 렌더링

기존과의 변경점:
- ❌ `formatBodyHtml()` 함수 미사용 - 마크다운 변환 제거
- ❌ `escapeHtml()` 미적용 - body HTML은 실제 렌더링
- ✅ 기존 발행된 글의 이미지는 그대로 표시
- ✅ 새 이미지는 Base64 data URL로 저장

## ⚠️ 주의사항

### 1. 이미지 크기 제한
- 매우 큰 이미지는 Base64로 변환 시 localStorage 용량 초과 가능
- 권장: 이미지를 1200x675px 정도로 사전 압축

### 2. 서버 업로드 미지원
- 현재 클라이언트 저장만 지원
- Cloudflare Pages 정적 사이트에 최적화됨
- 이미지 저장 없이 순수 HTML만 배포 가능

### 3. 마크다운 형식 변환 제거
- 이전: 마크다운 입력 → HTML 변환
- 현재: 직접 HTML 편집
- 마크다운 형식 텍스트는 그대로 텍스트로 저장됨

### 4. 이전 마크다운 글의 업그레이드
기존 마크다운 형식으로 저장된 글을 수정할 때:
```
1. "수정" 버튼 클릭
2. 마크다운 텍스트가 편집기에 표시됨
3. 필요 시 수동으로 HTML로 포맷팅
4. 또는 HTML로 변환한 후 저장
```

## 🔄 흐름 예시

### 예시 1: 새 가이드 글 작성

```
1. "새 글 작성" → 편집기 열림
2. 제목: "정수기 렌탈 선택 기준"
3. 슬러그: "water-purifier-guide" (자동)
4. 카테고리: "상품군별 핵심 비교"
5. 본문 작성 시작:
   - 텍스트 입력
   - 파일 선택 버튼으로 이미지 삽입
   - 이미지 위치 조정 (필요시 삭제 후 재삽입)
6. 미리보기로 확인
7. "저장 후 발행" 클릭
8. /blog/water-purifier-guide/index.html에 배포
```

### 예시 2: 기존 글 이미지 추가

```
1. 글 목록에서 "수정" 클릭
2. 기존 제목과 본문이 표시됨
3. 본문 중간에 커서 위치
4. Ctrl+V로 이미지 붙여넣기
5. "저장" 클릭
6. 블로그에서 새 이미지와 함께 표시
```

## 📊 검증 체크리스트

글 저장 후 다음을 확인하세요:

- [ ] 제목이 정상 표시됨
- [ ] 본문 텍스트가 올바르게 렌더링됨
- [ ] 이미지가 모두 표시됨
- [ ] 이미지 위치가 올바름
- [ ] 링크가 정상 작동
- [ ] 메타 설명이 검색 결과에 표시됨
- [ ] HTML 다운로드 후 브라우저에서 정상 표시됨

## 🆘 문제 해결

### 이미지가 표시되지 않는 경우
```
1. 개발자 도구 (F12) → Console 탭 확인
2. localStorage 용량 확인: 
   localStorage.getItem('rentalclearPosts')의 크기 확인
3. 브라우저 캐시 삭제 후 재시도
```

### 글이 저장되지 않는 경우
```
1. 브라우저 개발자 도구 → Application → LocalStorage 확인
2. 필수 필드 입력 여부 확인:
   - 제목 필수
   - 슬러그 필수
   - 본문 (선택)
3. 콘솔 에러 메시지 확인
```

### Base64 이미지 크기가 너무 큰 경우
```
1. 이미지 압축 도구 사용 (예: TinyPNG)
2. 해상도 축소 (1200x675px 권장)
3. 여러 이미지 분산 저장
```

## 📦 수정된 파일 목록

1. **assets/admin.js**
   - `postToHtml()` - body 렌더링 방식 변경
   - `openEditor()` - HTML 직접 렌더링
   - `hydratePost()` - HTML 유지
   - `extractBodyHtml()` - 새 함수 추가
   - `bindEvents()` - 파일 선택 이벤트 추가

2. **admin/index.html**
   - 본문 편집 섹션에 `.editor-body-toolbar` 추가
   - 파일 선택 버튼 추가
   - 사용 설명 텍스트 추가

3. **assets/styles.css**
   - `.editor-body-toolbar` 스타일 추가
   - `.editor-body-toolbar button` 스타일 추가
   - `.editor-body-toolbar .small` 스타일 추가

## 🚀 배포 시 참고사항

Cloudflare Pages에 배포할 때:
1. 글 관리자에서 HTML 다운로드
2. `/blog/[slug]/index.html` 경로에 파일 배치
3. 이미지는 Base64로 인코딩되어 HTML 내부에 포함됨
4. 별도 이미지 파일 배포 불필요

## ✨ 앞으로의 개선 가능성

- [ ] 서버 업로드 지원 (S3, Cloudflare R2 등)
- [ ] 이미지 자동 최적화 및 압축
- [ ] 마크다운/HTML 혼합 지원
- [ ] 이미지 순서 드래그 앤 드롭
- [ ] 텍스트 포맷팅 도구 (굵게, 이탤릭 등)
- [ ] 링크 삽입 도구
- [ ] 테이블 편집기
